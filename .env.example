# Frontend
VITE_APP_NAME=Willovate Smart Slot
VITE_API_BASE_URL=http://localhost:5000
VITE_USE_MOCK_DATA=false

# Backend reference values for the required .NET 8 API, if added to this repository.
# Do not put real secrets in committed files.
ASPNETCORE_ENVIRONMENT=Development
ASPNETCORE_URLS=http://localhost:5000
DATABASE_PROVIDER=SqlServer
CONNECTION_STRING=Server=localhost;Database=WillovateSmartSlot;Trusted_Connection=True;TrustServerCertificate=True;
JWT_SECRET=replace-with-a-local-development-secret
